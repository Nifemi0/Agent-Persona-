# Dashboard (Next.js)

Skeleton for the Next.js dashboard to register, view, and verify personas.

- Pages:
  - /create — persona builder + wallet sign + pin to IPFS
  - /persona/[id] — display persona, verification status, attestations
  - /audit — list of audited personas

Next: run `npx create-next-app` in this directory or scaffold minimal files.
# Dashboard TODOs

- /create — persona builder form (name, description, contact, homepage, tags)
- wallet sign flow using wagmi/ethers
- pin to IPFS via web3.storage (if token present)
- call register via ethers/cast using browser wallet (sign_tx flow via backend)
- /persona/[id] — fetch onchain CID, download metadata, verify signature status, show badges
